import cookie from '@fastify/cookie'

export const autoConfig = {
  // Set plugin options here
}

/**
 * This plugin parses cookies from request headers.
 *
 * @see {@link https://github.com/fastify/fastify-cookie}
 */
export default cookie
